{
  "name": "GhostText-browser",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "chrome-webstore-upload-cli": "^1.0.0",
    "typescript": "^1.8.10"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "release": "tsc; webstore upload --source=browser --auto-publish"
  }
}
